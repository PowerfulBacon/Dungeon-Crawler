
name: Run Unity Tests
on: [push]
jobs:
  Run-Unity-Tests:
    runs-on: ubuntu-latest
    env:
        working-directory: './Dungeon Crawler'
    steps:
        working-directory: ${{env.working-directory}}
      - name: Unity - Test runner
        uses: game-ci/unity-test-runner@v2.0-alpha-6
